---
import Layout from '../layouts/Layout.astro';

const schema = {
  "@context": "https://schema.org",
  "@type": "AboutPage",
  "name": "About Pythia Insights",
  "description": "One-person consultancy delivering full-stack finance. FP&A experience meets software engineering principles.",
  "url": "https://pythiainsights.com/about"
};
---

<Layout 
  title="About" 
  description="One-person consultancy delivering full-stack finance. FP&A experience meets software engineering principles."
  canonicalUrl="https://pythiainsights.com/about"
  schema={schema}
>

<section class="hero-mini">
  <div class="hero-grid-bg"></div>
  <div class="hero-content reveal">
    <div class="hero-tag">// ABOUT</div>
    <h1>One consultant.<br><span class="accent">Full-stack capability.</span></h1>
    <p class="hero-desc">
      Pythia Insights is a one-person consultancy. No account managers, 
      no junior analysts, no overhead. Just direct access to someone who 
      actually does the work.
    </p>
  </div>
</section>

<div class="section-divider"></div>

<section class="section">
  <div class="about-content reveal">
    <div class="about-text">
      <div class="section-label">// The Person</div>
      <h2>Hi, I'm Eric.</h2>
      <p>
        I spent years in FP&A at growth-stage companies—building operating models, 
        preparing board decks, running scenario analyses, and wondering why everything 
        still lived in Excel.
      </p>
      <p>
        Then I learned to code. Suddenly, those bottoms-up revenue models could be 
        version-controlled. Those cohort analyses could auto-refresh. Those board 
        reports could deploy from Git.
      </p>
      <p>
        Pythia is the intersection of finance expertise and software engineering 
        principles. I don't just analyze your numbers—I build the infrastructure 
        that makes analysis automatic, reproducible, and scalable.
      </p>
    </div>
    <div class="about-credentials">
      <div class="credential-card">
        <div class="credential-icon">📊</div>
        <h4>FP&A Background</h4>
        <p>Years of operating model experience at growth-stage SaaS companies. ARR waterfalls, cohort analysis, board reporting.</p>
      </div>
      <div class="credential-card">
        <div class="credential-icon">🐍</div>
        <h4>Technical Skills</h4>
        <p>Python, SQL, dbt, modern BI tools. I build the data products, not just the analysis.</p>
      </div>
      <div class="credential-card">
        <div class="credential-icon">🎓</div>
        <h4>Education</h4>
        <p>MBA, University of Pittsburgh. CPA-track coursework.</p>
      </div>
    </div>
  </div>
</section>

<div class="section-divider"></div>

<section class="section">
  <div class="reveal">
    <div class="section-label">// Why "Pythia"</div>
    <div class="section-title">The Oracle of Delphi.</div>
    <p class="section-desc">
      In ancient Greece, the Pythia was the oracle—the one who could see patterns 
      others couldn't, who could translate chaos into insight.
    </p>
  </div>

  <div class="philosophy-grid reveal">
    <div class="philosophy-card">
      <h3>Data as Oracle</h3>
      <p>Your financial data contains answers to questions you haven't thought to ask yet. The job is building systems that surface those answers automatically.</p>
    </div>
    <div class="philosophy-card">
      <h3>Clarity Over Complexity</h3>
      <p>The goal isn't impressive models—it's decisions made confidently. If the output doesn't change behavior, the work was wasted.</p>
    </div>
    <div class="philosophy-card">
      <h3>Infrastructure > Insights</h3>
      <p>Individual insights fade. Infrastructure compounds. I build the second kind.</p>
    </div>
  </div>
</section>

<div class="section-divider"></div>

<section class="section">
  <div class="reveal">
    <div class="section-label">// How I Work</div>
    <div class="section-title">Direct, opinionated, transparent.</div>
  </div>

  <div class="working-style reveal">
    <div class="style-item">
      <span class="style-icon">💬</span>
      <div>
        <h4>Direct Communication</h4>
        <p>No account managers or project managers as intermediaries. You talk to the person doing the work.</p>
      </div>
    </div>
    <div class="style-item">
      <span class="style-icon">📝</span>
      <div>
        <h4>Opinionated Recommendations</h4>
        <p>I'll tell you what I think you should do. You're hiring for judgment, not just execution.</p>
      </div>
    </div>
    <div class="style-item">
      <span class="style-icon">🔍</span>
      <div>
        <h4>Transparent Pricing</h4>
        <p>Monthly retainer or project-based. No surprise invoices, no scope creep games.</p>
      </div>
    </div>
    <div class="style-item">
      <span class="style-icon">📚</span>
      <div>
        <h4>Knowledge Transfer</h4>
        <p>Everything I build is documented. If we stop working together, you own the infrastructure.</p>
      </div>
    </div>
  </div>
</section>

<div class="section-divider"></div>

<section class="section">
  <div class="reveal">
    <div class="section-label">// Good Fit</div>
    <div class="section-title">Who this is for.</div>
  </div>

  <div class="fit-grid reveal">
    <div class="fit-card good">
      <h3>Great Fit</h3>
      <ul>
        <li>Series A-C startups with real financial complexity</li>
        <li>Technical founders who value code over spreadsheets</li>
        <li>Companies that want to own their financial infrastructure</li>
        <li>Teams that need FP&A + BI + bookkeeping integrated</li>
        <li>Businesses scaling past what part-time bookkeeping handles</li>
      </ul>
    </div>
    <div class="fit-card bad">
      <h3>Not A Fit</h3>
      <ul>
        <li>Pre-revenue startups (you don't need this yet)</li>
        <li>Enterprise companies (you need a team, not a consultant)</li>
        <li>Anyone who wants me to maintain Excel models</li>
        <li>Clients who need 24/7 on-call availability</li>
        <li>Businesses looking for the cheapest option</li>
      </ul>
    </div>
  </div>
</section>

<div class="section-divider"></div>

<section class="cta-section">
  <div class="cta-glow"></div>
  <h2>Think we might be a fit?</h2>
  <p>Let's have a conversation. No pitch, no pressure.</p>
  <a href="/contact" class="btn-primary">Schedule a Discovery Call</a>
</section>

</Layout>

<style>
  .hero-mini {
    padding: 10rem 3rem 5rem;
    position: relative;
    overflow: hidden;
  }

  .hero-grid-bg {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(30, 30, 42, 0.3) 1px, transparent 1px),
      linear-gradient(90deg, rgba(30, 30, 42, 0.3) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse 70% 60% at 50% 30%, black 20%, transparent 70%);
  }

  .hero-content {
    max-width: 800px;
    position: relative;
    z-index: 2;
  }

  .hero-tag {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    color: var(--accent);
    letter-spacing: 0.08em;
    margin-bottom: 1.5rem;
  }

  .hero-mini h1 {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.03em;
    margin-bottom: 1.5rem;
  }

  .accent { color: var(--accent); }

  .hero-desc {
    font-size: 1.15rem;
    color: var(--text-muted);
    max-width: 600px;
    line-height: 1.7;
  }

  /* About content */
  .about-content {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: 4rem;
    max-width: 1100px;
    margin: 0 auto;
  }

  .about-text h2 {
    font-size: 2rem;
    margin-bottom: 1.5rem;
  }

  .about-text p {
    color: var(--text-muted);
    font-size: 1.05rem;
    line-height: 1.8;
    margin-bottom: 1.25rem;
  }

  .about-credentials {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .credential-card {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 1.5rem;
    transition: all 0.3s;
  }

  .credential-card:hover {
    border-color: var(--border-accent);
  }

  .credential-icon {
    font-size: 1.5rem;
    margin-bottom: 0.75rem;
  }

  .credential-card h4 {
    font-size: 1rem;
    margin-bottom: 0.5rem;
  }

  .credential-card p {
    color: var(--text-muted);
    font-size: 0.85rem;
    line-height: 1.6;
  }

  /* Philosophy grid */
  .philosophy-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    max-width: 1100px;
    margin: 0 auto;
  }

  .philosophy-card {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 2rem;
    transition: all 0.4s;
  }

  .philosophy-card:hover {
    border-color: var(--accent);
    transform: translateY(-3px);
    box-shadow: 0 12px 40px rgba(0,0,0,0.3);
  }

  .philosophy-card h3 {
    font-size: 1.1rem;
    margin-bottom: 0.75rem;
  }

  .philosophy-card p {
    color: var(--text-muted);
    font-size: 0.9rem;
    line-height: 1.65;
  }

  /* Working style */
  .working-style {
    max-width: 800px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .style-item {
    display: flex;
    align-items: flex-start;
    gap: 1.5rem;
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 1.5rem;
    transition: all 0.3s;
  }

  .style-item:hover {
    border-color: var(--border-accent);
  }

  .style-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .style-item h4 {
    font-size: 1rem;
    margin-bottom: 0.5rem;
  }

  .style-item p {
    color: var(--text-muted);
    font-size: 0.9rem;
    line-height: 1.6;
  }

  /* Fit grid */
  .fit-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    max-width: 900px;
    margin: 0 auto;
  }

  .fit-card {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 2rem;
  }

  .fit-card.good {
    border-color: var(--accent);
  }

  .fit-card.bad {
    border-color: var(--rose);
  }

  .fit-card h3 {
    font-size: 1.25rem;
    margin-bottom: 1.25rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--border);
  }

  .fit-card.good h3 { color: var(--accent); }
  .fit-card.bad h3 { color: var(--rose); }

  .fit-card ul {
    list-style: none;
  }

  .fit-card li {
    font-size: 0.9rem;
    color: var(--text-muted);
    padding: 0.5rem 0;
    padding-left: 1.5rem;
    position: relative;
  }

  .fit-card.good li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--accent);
    font-weight: 600;
  }

  .fit-card.bad li::before {
    content: '✕';
    position: absolute;
    left: 0;
    color: var(--rose);
    font-weight: 600;
  }

  /* CTA */
  .cta-section {
    text-align: center;
    padding: 8rem 3rem;
    position: relative;
  }

  .cta-glow {
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, var(--accent-glow), transparent 70%);
    filter: blur(100px);
    opacity: 0.5;
    pointer-events: none;
  }

  .cta-section h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    letter-spacing: -0.02em;
    margin-bottom: 1rem;
    position: relative;
  }

  .cta-section p {
    color: var(--text-muted);
    font-size: 1.1rem;
    margin-bottom: 2.5rem;
    position: relative;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .about-content { grid-template-columns: 1fr; gap: 3rem; }
    .philosophy-grid { grid-template-columns: 1fr; }
    .fit-grid { grid-template-columns: 1fr; }
  }

  @media (max-width: 768px) {
    .hero-mini { padding: 8rem 1.5rem 3rem; }
    .cta-section { padding: 4rem 1.5rem; }
  }
</style>
