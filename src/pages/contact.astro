---
import Layout from '../layouts/Layout.astro';

const schema = {
  "@context": "https://schema.org",
  "@type": "ContactPage",
  "name": "Contact Pythia Insights",
  "description": "Schedule a discovery call. Let's talk about your financial infrastructure.",
  "url": "https://pythiainsights.com/contact"
};
---

<Layout 
  title="Contact" 
  description="Schedule a discovery call. Let's talk about upgrading your financial infrastructure."
  canonicalUrl="https://pythiainsights.com/contact"
  schema={schema}
>

<section class="hero-mini">
  <div class="hero-grid-bg"></div>
  <div class="hero-glow"></div>
  <div class="hero-content reveal">
    <div class="hero-tag">// CONTACT</div>
    <h1>Let's <span class="accent">talk.</span></h1>
    <p class="hero-desc">
      No pitch deck, no sales pressure. Just a conversation about 
      your business and whether we'd be a good fit.
    </p>
  </div>
</section>

<div class="section-divider"></div>

<section class="section">
  <div class="contact-layout">
    <div class="contact-form-container reveal">
      <div class="form-header">
        <h2>Send a message</h2>
        <p>Or just book a call directly—link is on the right.</p>
      </div>
      
      <form class="contact-form" action="https://formspree.io/f/xkgnqkdv" method="POST">
        <div class="form-row">
          <div class="form-group">
            <label for="name">Name</label>
            <input type="text" id="name" name="name" required placeholder="Your name">
          </div>
          <div class="form-group">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" required placeholder="you@company.com">
          </div>
        </div>
        
        <div class="form-group">
          <label for="company">Company</label>
          <input type="text" id="company" name="company" placeholder="Company name (optional)">
        </div>
        
        <div class="form-group">
          <label for="interest">What are you looking for?</label>
          <select id="interest" name="interest">
            <option value="">Select an option</option>
            <option value="bookkeeping">Bookkeeping / Foundation</option>
            <option value="fpa">FP&A / Analysis</option>
            <option value="bi">Custom BI / Dashboards</option>
            <option value="modeling">Python Modeling</option>
            <option value="fractional">Fractional CFO</option>
            <option value="fullstack">Full Stack (all of the above)</option>
            <option value="other">Something else</option>
          </select>
        </div>
        
        <div class="form-group">
          <label for="message">Tell me about your situation</label>
          <textarea id="message" name="message" rows="5" placeholder="What's your current setup? What challenges are you facing? What does success look like?"></textarea>
        </div>
        
        <button type="submit" class="btn-primary submit-btn">
          Send Message
          <span class="btn-arrow">→</span>
        </button>
      </form>
    </div>

    <div class="contact-info reveal">
      <div class="info-card primary">
        <div class="info-icon">📅</div>
        <h3>Book a Discovery Call</h3>
        <p>30 minutes to talk through your needs. No prep required—just show up and let's chat.</p>
        <a href="https://cal.com/pythia-insights/discovery" target="_blank" rel="noopener" class="btn-primary">
          Schedule Now
        </a>
      </div>

      <div class="info-card">
        <div class="info-icon">📧</div>
        <h3>Email</h3>
        <p>Prefer email? That works too.</p>
        <a href="mailto:eric@pythiainsights.com" class="info-link">eric@pythiainsights.com</a>
      </div>

      <div class="info-card">
        <div class="info-icon">💬</div>
        <h3>Response Time</h3>
        <p>I typically respond within 24 hours on business days.</p>
      </div>

      <div class="info-card">
        <div class="info-icon">🌎</div>
        <h3>Location</h3>
        <p>Based in the US, working with clients across time zones. Remote-first by design.</p>
      </div>
    </div>
  </div>
</section>

<div class="section-divider"></div>

<section class="section">
  <div class="reveal">
    <div class="section-label">// Before We Talk</div>
    <div class="section-title">What to expect.</div>
  </div>

  <div class="expect-grid reveal">
    <div class="expect-item">
      <div class="expect-number">01</div>
      <h4>Discovery Call</h4>
      <p>30 minutes to understand your situation. What's working, what's not, what you're trying to achieve.</p>
    </div>
    <div class="expect-item">
      <div class="expect-number">02</div>
      <h4>Scoping</h4>
      <p>If there's a fit, I'll put together a proposal. Clear scope, clear pricing, clear timeline.</p>
    </div>
    <div class="expect-item">
      <div class="expect-number">03</div>
      <h4>Kickoff</h4>
      <p>We align on priorities, set up communication channels, and get to work.</p>
    </div>
    <div class="expect-item">
      <div class="expect-number">04</div>
      <h4>Delivery</h4>
      <p>Regular updates, documented work, and infrastructure you own at the end.</p>
    </div>
  </div>
</section>

</Layout>

<style>
  .hero-mini {
    padding: 10rem 3rem 5rem;
    position: relative;
    overflow: hidden;
  }

  .hero-grid-bg {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(30, 30, 42, 0.3) 1px, transparent 1px),
      linear-gradient(90deg, rgba(30, 30, 42, 0.3) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse 70% 60% at 50% 30%, black 20%, transparent 70%);
  }

  .hero-glow {
    position: absolute;
    width: 400px;
    height: 400px;
    border-radius: 50%;
    background: radial-gradient(circle, var(--accent-glow), transparent 70%);
    top: 20%;
    right: 20%;
    filter: blur(80px);
  }

  .hero-content {
    max-width: 600px;
    position: relative;
    z-index: 2;
  }

  .hero-tag {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    color: var(--accent);
    letter-spacing: 0.08em;
    margin-bottom: 1.5rem;
  }

  .hero-mini h1 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.03em;
    margin-bottom: 1.5rem;
  }

  .accent { color: var(--accent); }

  .hero-desc {
    font-size: 1.15rem;
    color: var(--text-muted);
    max-width: 500px;
    line-height: 1.7;
  }

  /* Contact layout */
  .contact-layout {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: 4rem;
    max-width: 1100px;
    margin: 0 auto;
  }

  /* Form */
  .contact-form-container {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 2.5rem;
  }

  .form-header {
    margin-bottom: 2rem;
  }

  .form-header h2 {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
  }

  .form-header p {
    color: var(--text-muted);
    font-size: 0.9rem;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }

  .form-group {
    margin-bottom: 1.5rem;
  }

  .form-group label {
    display: block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.7rem;
    color: var(--text-dim);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    margin-bottom: 0.5rem;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    width: 100%;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 0.85rem 1rem;
    color: var(--text);
    font-family: 'Outfit', sans-serif;
    font-size: 0.95rem;
    transition: all 0.3s;
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--accent);
    box-shadow: 0 0 0 3px var(--accent-dim);
  }

  .form-group input::placeholder,
  .form-group textarea::placeholder {
    color: var(--text-dim);
  }

  .form-group select {
    cursor: pointer;
  }

  .form-group textarea {
    resize: vertical;
    min-height: 120px;
  }

  .submit-btn {
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    margin-top: 1rem;
  }

  .btn-arrow {
    transition: transform 0.3s;
  }

  .submit-btn:hover .btn-arrow {
    transform: translateX(4px);
  }

  /* Info cards */
  .contact-info {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .info-card {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 1.5rem;
    transition: all 0.3s;
  }

  .info-card:hover {
    border-color: var(--border-accent);
  }

  .info-card.primary {
    border-color: var(--accent);
    background: linear-gradient(135deg, var(--accent-dim) 0%, var(--bg-elevated) 50%);
  }

  .info-icon {
    font-size: 1.5rem;
    margin-bottom: 0.75rem;
  }

  .info-card h3 {
    font-size: 1rem;
    margin-bottom: 0.5rem;
  }

  .info-card p {
    color: var(--text-muted);
    font-size: 0.85rem;
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  .info-card .btn-primary {
    width: 100%;
    text-align: center;
  }

  .info-link {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.85rem;
    color: var(--accent);
    transition: opacity 0.3s;
  }

  .info-link:hover {
    opacity: 0.8;
  }

  /* Expect grid */
  .expect-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
    max-width: 1100px;
    margin: 2rem auto 0;
  }

  .expect-item {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 1.5rem;
    transition: all 0.3s;
    position: relative;
  }

  .expect-item:hover {
    border-color: var(--border-accent);
  }

  .expect-item::after {
    content: '→';
    position: absolute;
    right: -12px;
    top: 50%;
    transform: translateY(-50%);
    color: var(--text-dim);
    font-size: 1rem;
    z-index: 2;
  }

  .expect-item:last-child::after {
    display: none;
  }

  .expect-number {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.65rem;
    color: var(--accent);
    letter-spacing: 0.1em;
    margin-bottom: 0.75rem;
  }

  .expect-item h4 {
    font-size: 1rem;
    margin-bottom: 0.5rem;
  }

  .expect-item p {
    color: var(--text-muted);
    font-size: 0.85rem;
    line-height: 1.6;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .contact-layout { grid-template-columns: 1fr; gap: 2rem; }
    .contact-info { flex-direction: row; flex-wrap: wrap; }
    .info-card { flex: 1 1 200px; }
    .expect-grid { grid-template-columns: repeat(2, 1fr); }
    .expect-item::after { display: none; }
  }

  @media (max-width: 768px) {
    .hero-mini { padding: 8rem 1.5rem 3rem; }
    .form-row { grid-template-columns: 1fr; }
    .contact-info { flex-direction: column; }
    .expect-grid { grid-template-columns: 1fr; }
    .contact-form-container { padding: 1.5rem; }
  }
</style>
